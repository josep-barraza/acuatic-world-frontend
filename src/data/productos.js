      {/* <div key={p.id} className={style.card}> */}
 {/*        <img
          src={`http://localhost:3000/public/${p.img}`}
          alt={p.nombre}
        />
        <h4>{p.nombre}</h4>
        <p className={style.categoria}>{p.categoria}</p>
        <p className={style.descripcion}>{p.descripcion}</p>
        <strong>${p.precio}</strong>
        <span>Stock: {p.stock}</span>
        <button type="button" className="btn btn-primary"><strong>AGREGAR A CARRITO</strong></button>
      </div>
 */}
